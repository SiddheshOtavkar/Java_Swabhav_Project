package com.solidPrinciples.miniproject;

public class Main {
	public static void main(String[] args) {
		// 1. Create an order
        Order order = new Order("ELECTRONICS", 1000);

        // 2. Choose concrete strategies (could come from config/UI)
        DiscountStrategy discount   = new ElectronicsDiscount();
        PaymentProcessor payment    = new CreditCardProcessor();
        Notifier notifier           = new EmailNotifier();

        // 3. Run workflow
        OrderProcessor processor =
	}
}
